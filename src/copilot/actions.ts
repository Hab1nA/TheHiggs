// ============================================================
// CopilotKit Actions — 安全 frontend actions（可选占位）
// ============================================================


/** 注册的 CopilotKit actions（第一版为空） */
export const copilotActions: Array<{
  name: string;
  description: string;
  handler: () => unknown;
}> = [
  {
    name: "inspect_runtime_state",
    description: "Return the current AUIR runtime state for debugging.",
    handler: () => "AUIR state inspection not yet wired.",
  },
];
